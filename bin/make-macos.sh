#!/bin/bash
# cd ~/.tmux && cat .tmux.conf.local tmux.conf.common tmux.conf.macos geovim.conf > ~/.tmux.conf.local
# cd ~/.tmux && cat .tmux.conf.local tmux.conf.common tmux.conf.macos tmux-prefix.conf tmux-lab.conf tmux-plugins.conf > ~/.tmux.conf.local
cd ~/.tmux && cat .tmux.conf.local tmux.conf.common tmux.conf.macos tmux-prefix.conf tmux-lab.conf > ~/.tmux.conf.local
